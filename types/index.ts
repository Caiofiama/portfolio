export interface Project {
  name: string;
  description: string;
  stack: string[];
  live: string;
  github: string;
  icon: string;
  gradient: string;
}

export interface SkillCategory {
  name: string;
  skills: string[];
}
